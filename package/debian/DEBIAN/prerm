#!/bin/sh
set -e

if [ -d /run/systemd/system ] && [ "$1" = remove ]; then
  deb-systemd-invoke stop sonos.service >/dev/null
  systemctl disable sonos.service || true
  rm /var/log/thingsplex/sonos/* || true
fi